'use strict';

// if (typeof(document) != 'undefined') {
  const AppHolder = document.querySelector('#app');

  const etchASketch = new EtchASketch(AppHolder, {width: 10, height: 10});
// } else {
//   module.exports = EtchASketch;
// }
